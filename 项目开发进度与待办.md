# 错题收集 HarmonyOS 应用——开发进度与待办

> 更新时间：2026-09-01（按主工程当前提交与复测结果刷新）
> 当前主工程：`G:\code\openHarmony\wrong_question_collection`
> 当前 HEAD：`fea5d10 refactor(app): complete server-first cutover`（Task 14）
> 技术栈：HarmonyOS API 26、ArkTS、ArkUI、NestJS、MySQL 8.4、Docker Compose

## 1. 当前结论

项目已有可编译的 API 26 端侧应用、华为账号登录代码、服务器优先数据仓库，以及已经部署运行的 MySQL 云端服务和已完成真实闭环的 Python OCR Worker。产品目标已落地为“服务器保存正式文字、客户端下载只读离线缓存；PDF 在服务器处理，题图只保存在发起导入的设备”。

当前状态：

- 生产 PDF 流程已经完全切换到云端 Worker：真实 PDF 已从 HarmonyOS 客户端上传，由真实 Ubuntu 服务器 Worker 完成 OCR/切题/裁图并进入 `review`。
- `OnDeviceOcrService` 与 `PdfImportCoordinator` 已从生产代码删除（Task 14，提交 `fea5d10`），生产目录扫描零命中。
- 普通数据 mutation 已切换服务器优先：`CloudQuestionRepository` → `/v1/sync/push` → server authoritative operation → 账号缓存；离线或未登录只允许浏览。
- 旧 RDB 表（`question_bank`、`question`、`wrong_question`、`question_image`）与 `sync_outbox` 仍保留一个恢复周期，但不再作为正常业务生产写入主路径；`SyncBootstrapService`/`SyncOutboxService` 仅保留 recovery-only 能力。
- 旧恢复数据尚未做破坏性删除（无 DROP TABLE、无清空 outbox、无删除旧图片）。

目前尚不能宣称“全流程已经真机验收”。`hdc list targets` 仍可能为 `[Empty]`，Account Kit 真实账号授权、私有 CA 真机握手、双设备交叉同步、离线只读和 24 小时清理等仍是 Task 15 上线前待办；但“客户端到云端 Worker 的真实链路”已有 2026-09-01 的真实服务器证据，不再视为缺失。

当前数据边界：MySQL 保存账号、题库文字、题目文字、错题状态和同步序列；服务器只临时保存上传 PDF、OCR 中间文件和待下载题图；题图下载成功并 ACK 后或超过 24 小时由服务器删除；设备按华为账号隔离保存题图和只读文字缓存。客户端和 Git 中不得保存华为 Client Secret、JWT 密钥、MySQL 密码、CA 私钥或服务器私钥。

| 模块 | 当前状态 | 说明 |
| --- | --- | --- |
| API 26 / ArkUI | 已实现并构建通过 | target/compatible SDK 26，启用官方 UIMaterial |
| 首页与四项底部导航 | 已实现并构建通过 | 首页、书籍、错题、我的；官方 Symbol 和沉浸光感悬浮材质 |
| 本地题库与错题 | 已实现 | ArkData RDB（schema v6）、编辑、统计、错题复习；JSON 导入已切换服务器优先 |
| PDF 端侧导入（旧回退链路） | 已停用（Task 14） | `OnDeviceOcrService`/`PdfImportCoordinator` 已删除，生产代码零引用；PDF 全部走云端 Worker |
| 华为账号登录 | 代码完成，待真实账号真机验收 | Account Kit 授权码交给服务端，不在客户端保存 Client Secret |
| 安全会话 | 已实现并构建通过 | Access Token 仅内存保存；Refresh Token 与 deviceKey 使用 Asset Store |
| 服务器优先文字数据 | 已实现并构建通过 | 在线 mutation 走 `CloudQuestionRepository` → `/v1/sync/push` → 成功后才更新账号缓存；读取走 `/v1/sync/pull` → 账号隔离只读缓存 |
| 旧 outbox 同步（recovery-only） | 保留，不再作为生产写路径 | `SyncOutboxService`/`SyncBootstrapService` 保留恢复能力；普通业务不再 enqueue |
| 旧本地数据迁移 | 已实现并构建通过 | 用户明确确认后幂等上传、反向核验、题图重绑；失败保留旧表与 outbox，可重试 |
| NestJS / MySQL 云端 | 已部署且健康 | 公网 IP `114.132.197.160`，MySQL 8.4、API、Caddy、备份、OCR Worker 容器 |
| 云端 PDF 导入后端 | 已完成真实闭环 | 上传、状态机、Worker 写回、草稿确认和题图下载；真实任务 `6347b1bb-…` 已处理到 `review` |
| 多设备端到端同步 | 待验收（Task 15） | 需要至少两台 API 26 设备和真实华为账号 |

## 2. 云端化改造任务总进度

正式设计：`docs/superpowers/specs/2026-08-26-server-pdf-processing-cloud-text-design.md`
实施计划：`docs/superpowers/plans/2026-08-26-server-pdf-processing-cloud-text-plan.md`

| 任务 | 状态 | 已完成或下一步 |
| --- | --- | --- |
| 1. 云端导入 MySQL schema | 已完成 | 已新增任务、分片、OCR 草稿和临时文件表；复合外键、CHECK 约束、迁移回滚和真实 MySQL 集成测试入口已完成。主流程复验：24/24 套件，85 项通过、1 项因本机无 MySQL 配置跳过，生产构建通过。 |
| 2. 安全导入文件存储 | 已完成 | 已实现私有上传根目录、符号链接防护、4MB 分片、流式 SHA-256、原子无覆盖发布、崩溃后幂等恢复、安全删除和失败关闭磁盘水位；主流程复验：25/25 套件，115 项通过、1 项真实 MySQL 测试跳过，生产构建通过。 |
| 3. 导入任务状态机与仓储 | 已完成 | 已实现闭合状态机、所有权与设备校验、分片幂等、`FOR UPDATE SKIP LOCKED` 领取、`claimedAt` 处理令牌、短事务批量过期、失败重试、取消竞争和草稿原子替换。主流程复验：26/26 套件，139 项通过、1 项真实 MySQL 测试跳过，生产构建通过。 |
| 4. 认证分片上传接口 | 已完成 | 主体接口、认证、限额、manifest-first、持久租约与可重试清理均已实现；已用等价 tombstone 机制关闭“发布 fence 通过后、异步 hard-link 完成前租约过期并被取消”的 check/use 竞态：分片行永不消费（持久 tombstone）、取消末尾目录清扫使迟到 hard-link 只能 ENOENT 失败或在清扫期间被回收。规格审查与质量审查双重复审通过；服务端全量 31 套件 270 项通过、1 项真实 MySQL 测试跳过，生产构建通过。 |
| 5. Python OCR Worker | 已完成 | `server/worker/main.py` 已接入真实 `pdf_pipeline.PdfPipeline`；Worker 从 MySQL claim queued 任务、解析 manifest-first 的 source.pdf 存储键、逐页更新 processing 进度（claimedAt 栅栏）、执行渲染/OCR/切题/裁图。PaddleOCR 3.7.0 使用 `predict()`；构建与生产构造器均显式 `enable_mkldnn=False`。本机 pytest 85 项通过、2 项 PDFium 容器测试跳过；真实容器构建与真实 MySQL 闭环已在 Ubuntu 服务器验证（见任务 8）。 |
| 6. 服务端 PDF/OCR/切题 | 已完成 | `question_parser.py` 切题/分类与 `pdf_pipeline.py` 两遍处理已接入生产 Worker：草稿与题图 artifact 行经 `replace_draft` 单事务原子写入（manifest-first，行先于字节）、成功 `processing→review`、失败按 `PipelineError`/`RetryablePipelineError` 分类（最多自动重试两次）。真实服务器已处理完整 PDF 任务（3 题 3 图，见任务 8）。 |
| 7. 草稿确认与图片清理 | 已完成 | 提交 `37b01bc`、`df72258`、`a9bb625` 实现草稿读取、确认、正式题库事务写入、原设备题图下载、精确 ACK 和过期清理；质量审查 5 项整改全部关闭（文件句柄幂等关闭、confirm 端点级 parser、UUID 小写统一、下载实哈希校验、keyset/复合分页）。规格、质量与主流程三重验证通过：服务端 34 套件、330 项通过、1 项真实 MySQL 历史跳过，生产构建通过。 |
| 8. Worker 部署边界 | 已完成 | 真实 Ubuntu 服务器复验通过：无缓存镜像构建成功、新容器启动成功、真实 PDF 任务闭环到 `review`；部署边界实机确认（无公网端口、内部网络、只读根、非 root、CapDrop、限额等）。详见第 5 节服务器证据。 |
| 9. HarmonyOS 云端导入传输 | 已完成 | 提交 `ad86b8d feat(app): upload PDFs to cloud imports`。实现 ArkTS 云端 PDF import API：分片上传、断点恢复、import job 状态轮询、草稿获取、artifact 下载、SHA-256/size 验证、账号与设备边界；所有请求复用安全会话与 401 单次刷新。 |
| 10. 账号隔离只读缓存 | 已完成 | 提交 `e425eae feat(app): add account scoped cloud cache`。schema v6 新增账号隔离的 server-authoritative 文字缓存（`cloud_cache_state`、题库/题目/错题缓存、`device_question_image`）；`DeviceImageStore` 负责设备题图与账号隔离；登出清文字缓存但保留隔离的本机题图。 |
| 11. PDF 页面切换到云端流程 | 已完成 | 提交 `35967cf feat(app): review cloud-processed PDF imports`。设置页改用云端任务、上传/处理进度、server OCR 状态轮询、review 页面读取服务器草稿、下载题图、校验后写入账号隔离设备目录、成功保存后 ACK；页面生产流程不再调用端侧 OCR/PDF pipeline。 |
| 12. 普通数据服务器优先 | 已完成 | 提交 `148030b feat(app): implement server-first question repository`。`CloudQuestionRepository` + `ConnectivityPolicy`：在线 mutation → `/v1/sync/push` → server authoritative operation → 账号缓存；读取 `/v1/sync/pull` → 账号隔离缓存；离线或未登录只允许浏览，编辑/删除/错题状态修改禁用。 |
| 13. 旧本地数据迁移 | 已完成 | 提交 `e70c408 feat(app): migrate local text to cloud safely`。`LegacyCloudMigrationService`/`LegacyMigrationModels`/`LegacyMigrationCard`：用户明确确认后才绑定当前华为账号；migration state 与 sync_outbox 分离；stable UUID 幂等上传；authoritative snapshot 反向核验；normalized text SHA-256；CloudCache 验证后更新；本机题图重新绑定到 accountId + authoritative question UUID；原图不删除；失败保留旧表与 outbox，可 resume/retry。 |
| 14. 停用端侧 OCR/outbox | 已完成 | 提交 `fea5d10 refactor(app): complete server-first cutover`。删除 `OnDeviceOcrService.ets`/`PdfImportCoordinator.ets`，生产扫描零命中；`QuestionBankService`/`WrongQuestionService` 不再 enqueue；生产启动不再自动调用旧 backfill；`CloudSyncService` 停用旧 outbox push/pull，改用 `CloudQuestionRepository`；旧表/sync_outbox/migration state/旧图片全部保留，无破坏性清理。 |
| 15. 部署与真机验收 | 未完成 | 全量回归、真实账号、断点上传、双设备、离线只读、24 小时清理、资源压力、备份恢复演练和上线前准备。下一阶段。 |

任务 1–4 行内测试数字是各任务完成时的历史快照；当前统一全量基线以第 5 节为准，统计格式固定为“套件数、通过数、跳过数、执行命令、对应 HEAD”。

### 完成情况汇总

- 已完成：任务 1–14，共 14 项。
- 进行中：无。
- 未完成：任务 15，共 1 项。
- 当前重点：最终部署/设备验收。
- 当前自动化基线：端侧 25/25 个 Node 契约文件通过（含 CloudCutoverContracts 10/10）；Hvigor 测试 102 项全部通过；API 26 HAP 构建成功；服务端 34/34 个 Jest 套件通过，330 项通过、1 项真实 MySQL 集成测试历史跳过；Worker pytest 85 项通过、2 项容器相关测试历史跳过；TypeScript 生产构建通过。
- 任务 4 完成条件已满足：发布与取消之间的 check/use 竞态已关闭，规格审查与质量审查双重复审通过，全量测试和生产构建通过。
- 任务 7 完成条件已满足：质量审查 5 项整改全部关闭，规格、质量和主流程三重验证通过。
- 任务 8 完成条件已满足：真实 Ubuntu 服务器完成无缓存构建、容器启动和真实 PDF 任务闭环，部署边界实机确认（详见第 5 节）。
- 任务 9–14 完成条件已满足：各任务契约/回归/构建通过并有对应提交（详见第 2 节表格与第 5 节基线）。

### 当前阻塞与风险

1. ~~任务 7 下载连接提前断开可能泄漏文件句柄。~~ 已关闭：控制器在数据库/文件查询前绑定 abort/close，`FileHandle` 在成功、失败、客户端断开时都幂等关闭，并有断开时序专项测试。
2. ~~任务 7 合法确认请求可能被默认 100 KiB JSON 限制提前拒绝。~~ 已关闭：confirm 路由走认证前置、端点级 `express.json`（默认 2 MiB），超限返回稳定 413 `CONFIRM_TOO_LARGE`。
3. ~~任务 7 嵌套 UUID 大小写未统一。~~ 已关闭：`draftQuestionId` 与 ACK `artifactIds` 在重复检测、业务比较与确认哈希计算前统一小写。
4. ~~任务 7 下载只校验数据库哈希格式和文件大小，没有校验文件实际 SHA-256。~~ 已关闭：下载流式计算实际 SHA-256 并与记录比对，同尺寸损坏文件返回 `FILE_HASH_MISMATCH`。
5. ~~任务 7 清理分页存在重复全量扫描。~~ 已关闭：清理改为 keyset/复合分页（parts/artifacts 按窗口查询）。
6. ~~Worker SQL snake_case 与迁移 camelCase 不一致。~~ 已关闭并有真实服务器证据：`Unknown column 'user_id'` 修复后，真实任务在迁移后的 camelCase schema（`userId`、`deviceId`、`bankName`、`progressCurrent`、`claimedAt` 等）上完成 claim → progress → source → replace_draft → review 闭环。
7. ~~无缓存 Worker 构建未验证。~~ 已关闭：服务器 `docker compose -p wqc build --no-cache ocr-worker` 真实成功。
8. ~~PaddleOCR Linux 生产验证缺失。~~ 已关闭：libGL.so.1、PaddleOCR 3.x `cls=True`、MKLDNN/oneDNN CPU 问题均已修复并有真实容器运行证据（日志 `ocr worker started`、`processed: 3 questions, 3 images`）。
9. ~~真实 MySQL Worker 闭环缺失。~~ 已关闭：真实 MySQL 查询确认 `status = review`、`progressCurrent = 1/1`、`retryCount = 0`、`errorCode = NULL`、`draft_questions = 3`、`source_pdf = 1`、`question_image = 3`。
10. **Task 15 真机与最终验收仍缺失（未完成）。** 尚未有充分证据的事项：Account Kit 真实账号最终授权验收、双设备文字同步、账号隔离跨设备验证、离线只读真机验证、断点上传/断网恢复验收、confirm + 题图下载 + ACK 最终验收、24 小时题图过期清理最终验收、服务器资源压力测试、备份/恢复演练、上线前完整回归。注意：本次已从 HarmonyOS 客户端提交真实云端 PDF 并由 Worker 处理到 `review`，客户端到 Worker 链路已有真实证据；但除仓库现有记录明确证明外，不得擅自宣称“全部真机验收完成”。

任务状态的唯一完成规则：先查看本文件；标记“已完成”的任务只做回归测试，不重复开发；标记“未完成”的任务按正式实施计划先写失败测试再实现；完成后必须把实际测试数量、构建结果、服务器或设备证据写回本文件。

## 3. 端侧既有能力

### 3.1 首页与导航

- 新增真实数据驱动的学习首页：待复习、错题总数、已掌握、掌握率、最近书籍和最近错题。
- 底部导航改为四项悬浮栏，顺序固定为“首页、书籍、错题、我的”。
- 使用 API 26 官方 `uiMaterial.ImmersiveMaterial`、系统 Symbol、选中绿色和未选中深灰。
- 首页、书籍、错题和我的页面保留独立刷新状态；前后台切换后可刷新数据并触发同步。

### 3.2 本地功能

- 题库、题目、搜索、编辑、删除、错题添加/移除、掌握状态和统计。
- JSON 题库导入：Task 14 起为服务器优先（`CloudQuestionRepository.createBank`，成功后进入账号缓存）。
- PDF 导入：生产路径全部走云端（上传 → 服务器 OCR → 草稿审核 → 题图下载 → ACK）；端侧 OCR/渲染管线已删除。
- 数据库当前为 schema version 6（Task 10 新增账号隔离缓存表，v5 旧表保留）。

### 3.3 华为账号与会话

- “我的”页面使用 Account Kit 官方登录组件获取一次性 `authorizationCode`。
- 客户端只向 `/v1/auth/huawei` 发送授权码、deviceKey 和设备名称。
- Access Token 只保存在内存；Refresh Token 与 deviceKey 使用 Asset Store。
- 支持令牌刷新单飞、401 后刷新并重试一次、退出登录和会话恢复。
- 登录失败或云服务不可用不会阻止本地功能启动。

### 3.4 同步机制

- 正常业务写（Task 12 起）：`CloudQuestionRepository` 在线 mutation → `POST /v1/sync/push` → 服务器返回 authoritative operation → 成功后才更新账号缓存；不写本地猜测数据，不产生新的 outbox 行。
- 正常读取：`GET /v1/sync/pull` 按账号 cursor 增量拉取 → 账号隔离只读缓存（`cloud_cache_state` 等 v6 表）。
- 离线或未登录：只允许浏览缓存，编辑、删除、错题状态变更必须禁用并给出提示。
- 旧 outbox 机制（recovery-only，保留但不再是生产写路径）：`sync_identity` 保持本地主键到服务器 UUID 的映射；`sync_outbox` 保留 schema；`SyncBootstrapService`/`SyncOutboxService` 保留恢复兼容能力，生产启动不再自动创建 legacy outbox。
- 冲突基础规则为服务器序列最后写入生效。

## 4. 云端既有能力

云端目录为工程内的 `server/`，生产服务器部署目录为 `/opt/wrong_question_collection/server`，Compose project 为 `wqc`。

当前生产服务器已提供健康、认证、同步和云端 PDF 导入接口，云端导入已投入真实使用（真实 PDF 任务已由 Worker 处理到 `review`）：

```text
GET  /health
GET  /health/ready
POST /v1/auth/huawei
POST /v1/auth/refresh
POST /v1/auth/logout
POST /v1/sync/push
GET  /v1/sync/pull
POST /v1/imports/pdf
PUT  /v1/imports/pdf/:jobId/parts/:partIndex
POST /v1/imports/pdf/:jobId/complete
GET  /v1/imports/pdf/:jobId
GET  /v1/imports/pdf/:jobId/draft
POST /v1/imports/pdf/:jobId/confirm
GET  /v1/imports/pdf/:jobId/artifacts/:artifactId
POST /v1/imports/pdf/:jobId/artifacts/ack
DELETE /v1/imports/pdf/:jobId
```

已经完成：

- NestJS、TypeORM、MySQL 8.4、Caddy、Docker Compose 和独立 OCR Worker 容器部署。
- 多用户数据隔离、华为标识加密保存、JWT Access/Refresh Token、Refresh Token 轮换。
- 题库、题目、错题状态和复习事件的增量同步数据模型。
- MySQL 不向公网发布 3306；公网业务入口仅为 HTTPS 443；Worker 无公开端口，只加入内部网络。
- 私有 CA 为公网 IP `114.132.197.160` 签发证书；端侧 HAP 内置公有证书 `ca.crt`，不包含 CA 私钥或服务器私钥。
- 服务器 `/health/ready` 已返回：`{"status":"ok","service":"wrong-question-api","database":"ready"}`。
- Client Secret 只属于服务器环境变量，不进入 HarmonyOS 客户端或 Git。
- 云端 PDF 导入真实闭环：上传 → 分片 → complete → Worker claim/OCR/切题/裁图 → `review` → 草稿审核 → confirm → 题图下载 → ACK；真实任务见第 5 节。

## 5. 2026-09-01 当前验证证据

以下结果均在主工程 `G:\code\openHarmony\wrong_question_collection` 本轮实际执行，不使用旧日志代替：

| 验证项 | 当前结果 | 结论 |
| --- | --- | --- |
| 端侧 Node 契约测试 | 25/25 个 `.test.cjs` 文件通过（含 CloudCutoverContracts 10/10） | 端侧行为契约全部通过 |
| ArkTS/Hypium | Hvigor `test` 返回 `BUILD SUCCESSFUL`；Tests run 102、Failure 0、Error 0、Pass 102、Ignore 0 | ArkTS 单元测试全部通过 |
| API 26 HAP | Hvigor `assembleHap` 返回 `BUILD SUCCESSFUL` | 调试 HAP 可生成（`entry-default-signed.hap`/`entry-default-unsigned.hap`） |
| NestJS Jest | 34/34 套件；330 项通过，1 项真实 MySQL 集成测试历史跳过 | 服务端回归通过 |
| NestJS 构建 | `tsc -p tsconfig.build.json` 退出码 0 | 服务端 TypeScript 生产构建通过 |
| Python Worker（本机历史基线） | 85 项通过，2 项容器相关测试历史跳过 | 本机 pytest 基线保持；真实生产验证以服务器证据为准（见下） |
| 服务器 Worker 真实闭环 | 无缓存构建成功、容器启动、真实 PDF 任务处理到 `review` | Task 8 前置条件满足，客户端→Worker 真实链路证据成立 |
| 真机连接 | `hdc list targets` 当前仍可能为 `[Empty]` | Task 15 真机验收未执行 |

### Task 8 真实服务器验收证据（2026-09-01）

部署目录 `/opt/wrong_question_collection/server`，Compose project `wqc`，服务状态：

- `api` healthy
- `mysql` healthy
- `caddy` running
- `backup` running
- `ocr-worker` running
- `migrate` 正常 `Exited (0)`

Worker 部署边界实机确认：

- 无公开端口
- `worker_backend` Internal=true
- `ReadonlyRootfs=true`
- `User=ocr`
- `CapDrop=["ALL"]`
- `SecurityOpt=["no-new-privileges:true"]`
- `NanoCpus=2000000000`（2 CPU）
- `Memory=3221225472`（3 GiB）

服务器资源验证：内存约 3.6 GiB；swap 约 1.9 GiB；清理 Docker build cache 后磁盘 40G 总量、11G 已用、27G 可用、30% 使用率。

真实执行：

1. `docker compose -p wqc build --no-cache ocr-worker` — 真实无缓存构建成功。
2. `docker compose -p wqc up -d --no-deps --force-recreate ocr-worker` — 新 Worker 容器成功启动。
3. Worker 日志：`ocr worker started`。
4. 从 HarmonyOS 客户端提交真实 PDF 云端导入，jobId = `6347b1bb-fac1-4df4-a22f-348871433c80`。
5. Worker 日志：`processed: 3 questions, 3 images`。
6. 真实 MySQL 查询确认：`status = review`、`progressCurrent = 1`、`progressTotal = 1`、`retryCount = 0`、`errorCode = NULL`、`draft_questions = 3`、`source_pdf artifact = 1`、`question_image artifact = 3`。
7. 真实 schema 确认使用 camelCase：`userId`、`deviceId`、`bankName`、`progressCurrent`、`claimedAt` 等。

由此关闭的服务器问题：libGL.so.1、PaddleOCR 3.x `cls=True`、MKLDNN/oneDNN CPU 问题、Worker SQL snake_case vs MySQL camelCase、Worker DB connection factory、source artifact 查询、`replace_draft`、`processing → review`。不再写“真实 MySQL 闭环仍缺失”。

说明：本机 pytest 的 2 项容器相关历史跳过与 Jest 的 1 项真实 MySQL 历史跳过是历史基线；这些“历史 skip”不能再被写成“因此生产 Worker 未验证”——现有独立的真实 Ubuntu + MySQL + PaddleOCR + PDF 实际任务成功证据。

### 端侧各任务验证基线（历史快照）

- Task 12（`148030b`）：ServerFirstContracts 8/8 PASS；全部 CJS 23/23 PASS；Hvigor Tests run 94、Failure 0、Error 0、Pass 94；assembleHap BUILD SUCCESSFUL。
- Task 13（`e70c408`）：LegacyCloudMigrationContracts 17/17 PASS；全部 CJS 24/24 PASS；Hvigor Tests run 102、Failure 0、Error 0、Pass 102；assembleHap BUILD SUCCESSFUL。
- Task 14（`fea5d10`）：CloudCutoverContracts 10/10 PASS；全部 CJS 25/25 PASS；Hvigor Tests run 102、Failure 0、Error 0、Pass 102；assembleHap BUILD SUCCESSFUL。

当前 ArkTS 构建仍输出大量“函数可能抛异常”警告，并有 `getContext` 已弃用提示。它们没有阻止 HAP 产出，但应在 Task 15 修改对应文件时按模块清理，最终 release 构建前必须重新统计和确认。

## 6. 上线前待办（Task 15）

### P0：真机与最终验收（未完成）

- [ ] 使用真实华为账号完成 Account Kit 授权、服务端注册、刷新令牌和退出登录的最终验收。
- [ ] 真机请求 `https://114.132.197.160`，确认 HAP 内置私有 CA 能完成 TLS 校验。
- [ ] 真机完成分片上传、断点续传、断网恢复、服务器 OCR、在线审核、确认、题图下载、哈希校验和 ACK 的最终验收。
- [ ] 第二台设备登录同一账号，验证文字题库和错题状态 pull；确认原 PDF 不作为跨设备资料同步，题图只在发起导入并完成下载的设备可用。
- [ ] 验证账号 A/B 在同一设备的文字缓存和题图映射隔离，退出后离线缓存行为符合任务 10 设计。
- [ ] 验证断网后文字缓存可读、所有写按钮禁用；恢复网络后刷新正常。
- [ ] 验证 24 小时超时、删除失败重试、API 重启恢复和磁盘压力拒绝新导入；不得影响正式文字与健康检查。
- [ ] 服务器资源压力测试（按每任务三份 200 MiB 级预算）与 MySQL 备份/恢复演练，不只检查备份文件生成。

### P1：发布准备

- [ ] 在 AppGallery Connect 核对包名、签名证书 SHA-256、API 权限和华为登录配置。
- [ ] 使用真实参考 PDF 做 OCR 准确率、跨页切题、取消、内存峰值和长时间运行测试。
- [ ] 补充隐私政策和账号说明：华为标识及学习文字长期保存在自建服务器；PDF、OCR 中间文件和待下载题图会临时上传/生成并在 ACK 或 24 小时后删除；题图长期仅保存在发起设备。
- [ ] 正式发布前优先申请域名并切换到公共受信任 CA；当前公网 IP 私有 CA 方案需要随 HAP 更新证书。
- [ ] 清理现有 ArkTS 编译警告，并执行 release 签名构建和发布包验收。

### P2：后续增强

- [ ] 同步历史、冲突提示、失败详情和按项重试。
- [ ] 云端账号注销、数据导出和数据删除。
- [ ] 异机加密备份、磁盘/接口告警、MySQL 慢查询监控。
- [ ] 根据真机样本继续优化数学 OCR 与复杂版式切题。

## 7. 下一步操作

正式逐项实施细节以 `docs/superpowers/plans/2026-08-26-server-pdf-processing-cloud-text-plan.md` 为准。任务 1–14 已完成并验证（详见第 2 节与第 5 节）；剩余为任务 15。

### 7.1 任务 1–8 服务器侧（已完成）

- ✅ 任务 1–4：云端导入 schema、安全存储、状态机仓储、认证分片上传（服务端契约与回归通过）。
- ✅ 任务 5–6：Worker claim/进度/草稿/裁图链路（本机 pytest 85 项通过 + 真实服务器闭环）。
- ✅ 任务 7：草稿确认、题图下载、ACK 与清理（34/34 套件、330 项通过）。
- ✅ 任务 8：真实服务器无缓存构建 `docker compose -p wqc build --no-cache ocr-worker` 成功；`docker compose -p wqc up -d --no-deps --force-recreate ocr-worker` 启动成功；真实 PDF 任务 `6347b1bb-fac1-4df4-a22f-348871433c80` 处理到 `review`（3 题 3 图，camelCase schema 全链路确认）；部署边界实机确认（无端口/内部网络/只读根/非 root/CapDrop/限额）。

### 7.2 任务 9–14 端侧（已完成）

- ✅ 任务 9（`ad86b8d`）：ArkTS 云端 PDF import 传输（分片上传、断点恢复、轮询、草稿、下载、校验、ACK）。
- ✅ 任务 10（`e425eae`）：schema v6 账号隔离只读缓存与设备题图映射。
- ✅ 任务 11（`35967cf`）：PDF 页面全部切换云端流程，生产页面不再调用端侧 OCR/渲染。
- ✅ 任务 12（`148030b`）：`CloudQuestionRepository`/`ConnectivityPolicy`，普通数据服务器优先、离线只读。
- ✅ 任务 13（`e70c408`）：`LegacyCloudMigrationService` 明确确认后幂等迁移旧文字并重绑题图，失败保留旧表。
- ✅ 任务 14（`fea5d10`）：删除 `OnDeviceOcrService`/`PdfImportCoordinator`，生产零引用；普通业务不再 enqueue；旧表/sync_outbox/migration state/旧图片全部保留，无破坏性清理。

### 7.3 第七步：任务 15，最终部署与真机验收（未完成）

1. 部署前检查 Ubuntu 至少 2 核、4 GB RAM（或 RAM 加受控 swap）和 `/opt/wrong_question_collection/server` 所在磁盘至少 15 GB 可用空间（当前实测 27G 可用）；先完成 MySQL 逻辑备份与配置备份。当前单 PDF 上限是 200 MiB、默认分片 4 MiB、磁盘最小保留水位 5 GiB、Worker 单并发。部署前还要定义每用户/全局活动任务数上限，并按每个任务至少“分片 + 合并 PDF + OCR 中间文件 + 题图”三份 200 MiB 级预算做压力测试。
2. 用测试账号完成一份小 PDF 的 create → upload → complete → OCR → review → confirm → 原设备下载 → ACK；核对下载哈希，ACK 后临时文件不可再下载而正式文字仍存在（真实服务器已闭环到 review，剩余 confirm/下载/ACK 环节待真机最终验收）。
3. 第二台设备登录同一账号，只能同步文字，不能下载第一台设备的临时题图；不同账号在服务端和同一设备上都必须完全隔离。
4. 验证断网后文字缓存可读、所有写按钮禁用；恢复网络后刷新正常。验证同账号退出再登录题图可见，不同账号不可见。
5. 用可注入时钟或隔离环境短 TTL 验证 24 小时清理、孤儿目录限制和磁盘压力拒绝新导入；正式题库文字与健康检查不能受影响。
6. 真机操作顺序：

```powershell
& 'D:\Program Files\Huawei\DevEco Studio1\sdk\default\openharmony\toolchains\hdc.exe' list targets
& 'D:\Program Files\Huawei\DevEco Studio1\sdk\default\openharmony\toolchains\hdc.exe' install -r `
  'G:\code\openHarmony\wrong_question_collection\entry\build\default\outputs\default\entry-default-signed.hap'
& 'D:\Program Files\Huawei\DevEco Studio1\sdk\default\openharmony\toolchains\hdc.exe' shell aa start `
  -a EntryAbility -b com.gfs.wrongquestion
```

7. 发现崩溃时先保存 `hdc hilog` 和 faultlogger 证据，不得通过删除数据库掩盖升级或同步问题。
8. 最终把测试数量、容器状态、设备 ID、OCR 耗时/内存/磁盘、双设备结果和未验证项写回本文件；没有观察证据的项目不得勾选完成。

### 7.4 每个任务完成后的固定收尾

1. 重新读取本文件，确认本任务当前不是“已完成”；若已完成，仅复测。
2. 运行该任务的 focused tests、全量回归、生产构建和 `git diff --check`。
3. 检查 `git status --short`，只暂存该任务文件；保留当前 `entry/` 的用户未提交修改、其他工程目录和部署压缩包。
4. 扫描 `.env`、私钥、PDF、题图、模型缓存、备份、签名材料和 Client Secret，确认都未进入 Git。
5. 更新本文件中的状态、实际证据、剩余风险和下一步，然后再提交文档。
