# 错题收集 HarmonyOS 应用——开发进度与待办

> 更新时间：2026-08-28（按主工程当前提交与复测结果刷新）
> 当前主工程：`G:\code\openHarmony\wrong_question_collection`
> 技术栈：HarmonyOS API 26、ArkTS、ArkUI、NestJS、MySQL 8.4、Docker Compose

## 1. 当前结论

项目已有可编译的 API 26 端侧应用、华为账号登录代码、文字数据同步客户端，以及已经部署运行的 MySQL 云端服务。产品目标现已调整为“服务器保存正式文字、客户端下载只读离线缓存；PDF 在服务器处理，题图只保存在发起导入的设备”。旧版端侧 PDF/OCR 和本地优先写入流程仍作为安全回退保留，只有云端导入、账号隔离缓存、旧数据迁移和真机验收全部通过后才允许停用。

目前尚不能宣称“全流程已经真机验收”。本轮 `hdc list targets` 返回 `[Empty]`，因此 Account Kit 真实授权、私有 CA 真机握手、PDF/OCR 性能和两台设备交叉同步仍是上线前待办。

当前数据边界已经确定：MySQL 保存账号、题库文字、题目文字、错题状态和同步序列；服务器只临时保存上传 PDF、OCR 中间文件和待下载题图；题图下载成功并 ACK 后或超过 24 小时由服务器删除；设备按华为账号隔离保存题图和只读文字缓存。客户端和 Git 中不得保存华为 Client Secret、JWT 密钥、MySQL 密码、CA 私钥或服务器私钥。任务 7 当前代码在模块启动后每 5 分钟触发一次清理，使用服务器 UTC 时间、1 小时 orphan grace、每页 32 条且每轮最多 32 页；删除失败会保留数据库记录并在后续轮次重试，但告警/指标和线性分页整改尚未完成。

| 模块 | 当前状态 | 说明 |
| --- | --- | --- |
| API 26 / ArkUI | 已实现并构建通过 | target/compatible SDK 26，启用官方 UIMaterial |
| 首页与四项底部导航 | 已实现并构建通过 | 首页、书籍、错题、我的；官方 Symbol 和沉浸光感悬浮材质 |
| 本地题库与错题 | 已实现 | ArkData RDB、JSON 导入、编辑、统计、错题复习 |
| PDF 端侧导入（旧回退链路） | 代码完成，待真机验收 | 端侧渲染、OCR、切题、裁图，不上传 PDF/题图；任务 11–14 完成后退出生产路径 |
| 华为账号登录 | 代码完成，待真实账号真机验收 | Account Kit 授权码交给服务端，不在客户端保存 Client Secret |
| 安全会话 | 已实现并构建通过 | Access Token 仅内存保存；Refresh Token 与 deviceKey 使用 Asset Store |
| 文字数据同步（旧 outbox 模式） | 已实现并构建通过 | outbox、cursor、push/pull、401 单次刷新、远端事务应用；不等于任务 12 的服务器优先/离线只读模式 |
| 旧数据首次同步 | 已补齐 | 首次升级时将已有非示例题库文字幂等加入 outbox；排除图片和内置示例 |
| NestJS / MySQL 云端 | 已部署且健康 | 公网 IP `114.132.197.160`，MySQL 8.4、API、Caddy、备份容器 |
| 云端 PDF 导入后端 | 开发中，不可部署 | 上传、状态机、草稿确认和题图下载主体已存在；Worker 写回链路和任务 7 质量问题仍未关闭 |
| 多设备端到端同步 | 待验收 | 需要至少两台 API 26 设备和真实华为账号 |

## 2. 云端化改造任务总进度

正式设计：`docs/superpowers/specs/2026-08-26-server-pdf-processing-cloud-text-design.md`
实施计划：`docs/superpowers/plans/2026-08-26-server-pdf-processing-cloud-text-plan.md`

| 任务 | 状态 | 已完成或下一步 |
| --- | --- | --- |
| 1. 云端导入 MySQL schema | 已完成 | 已新增任务、分片、OCR 草稿和临时文件表；复合外键、CHECK 约束、迁移回滚和真实 MySQL 集成测试入口已完成。主流程复验：24/24 套件，85 项通过、1 项因本机无 MySQL 配置跳过，生产构建通过。 |
| 2. 安全导入文件存储 | 已完成 | 已实现私有上传根目录、符号链接防护、4MB 分片、流式 SHA-256、原子无覆盖发布、崩溃后幂等恢复、安全删除和失败关闭磁盘水位；主流程复验：25/25 套件，115 项通过、1 项真实 MySQL 测试跳过，生产构建通过。 |
| 3. 导入任务状态机与仓储 | 已完成 | 已实现闭合状态机、所有权与设备校验、分片幂等、`FOR UPDATE SKIP LOCKED` 领取、`claimedAt` 处理令牌、短事务批量过期、失败重试、取消竞争和草稿原子替换。主流程复验：26/26 套件，139 项通过、1 项真实 MySQL 测试跳过，生产构建通过。 |
| 4. 认证分片上传接口 | 已完成 | 主体接口、认证、限额、manifest-first、持久租约与可重试清理均已实现；已用等价 tombstone 机制关闭“发布 fence 通过后、异步 hard-link 完成前租约过期并被取消”的 check/use 竞态：分片行永不消费（持久 tombstone）、取消末尾目录清扫使迟到 hard-link 只能 ENOENT 失败或在清扫期间被回收。规格审查与质量审查双重复审通过；服务端全量 31 套件 270 项通过、1 项真实 MySQL 测试跳过，生产构建通过。 |
| 5. Python OCR Worker | 进行中（框架完成，生产接线与容器验证未完成） | 已有单并发任务循环、claimedAt 栅栏、重试和协作停机；但 `server/worker/main.py` 仍使用占位 `PdfPipeline`，`job_store.py` 尚未把真实草稿和题图清单完整写回 MySQL。还需生成 Python 3.11/Linux hashed 锁文件并完成容器构建。 |
| 6. 服务端 PDF/OCR/切题 | 进行中（算法本地测试通过，端到端写回未完成） | `question_parser.py` 与 `pdf_pipeline.py` 已覆盖切题、题型、置信度、公式审查、逐页释放、裁图和错误分类；本轮本机 pytest 为 48 项通过、2 项容器集成测试跳过。下一步是接入任务 5 的真实 Worker、manifest-first 落盘并事务写入草稿/题图元数据。 |
| 7. 草稿确认与图片清理 | 进行中（主体已实现，质量审查未通过） | 提交 `37b01bc`、`df72258`、`a9bb625` 已实现草稿读取、确认、正式题库事务写入、原设备题图下载、精确 ACK 和过期清理；规格复审通过，服务端 33 套件、316 项通过、1 项跳过，但质量审查仍有 4 个 Important 和 1 个 Minor，修复并复审前不得标记完成。 |
| 8. Worker 部署边界 | 未开始 | 更新 Compose/Caddy，Worker 无公网端口、无账号密钥、只读根文件系统、内部网络和 CPU/内存限制。 |
| 9. HarmonyOS 云端导入传输 | 未开始 | 实现分片上传、断点续传、任务轮询、草稿和题图下载的严格 ArkTS 客户端。 |
| 10. 账号隔离只读缓存 | 未开始 | 本地 schema v6 新增文字缓存和设备题图映射；退出登录清文字缓存但保留并隔离本机题图。 |
| 11. PDF 页面切换到云端流程 | 未开始 | 设置页、进度页和审核页改用服务器任务，不再调用端侧 OCR/渲染。 |
| 12. 普通数据服务器优先 | 未开始 | 在线修改直接提交 `/v1/sync/push`，成功后更新缓存；离线只允许查看并禁用写操作。 |
| 13. 旧本地数据迁移 | 未开始 | 登录后幂等上传旧文字、核验摘要、重绑本机题图；失败保留旧库并允许重试。 |
| 14. 停用端侧 OCR/outbox | 未开始 | 只有任务 1–13 端到端通过后才移除生产引用；旧表保留一个版本用于恢复。 |
| 15. 部署与真机验收 | 未开始 | 全量测试、云服务器资源检查与部署、真实账号、断点上传、双设备、离线只读和 24 小时清理验收。 |

任务 1–4 行内测试数字是各任务完成时的历史快照；当前统一全量基线以第 5 节为准，统计格式固定为“套件数、通过数、跳过数、执行命令、对应 HEAD”。

### 完成情况汇总

- 已完成：任务 1–4，共 4 项。
- 进行中：任务 5、任务 6、任务 7，共 3 项。
- 未开始：任务 8–15，共 8 项。
- 当前自动化基线：端侧 19 个 Node 契约文件通过；Hvigor 测试与 API 26 HAP 构建均成功；服务端 33/33 个 Jest 套件通过，316 项通过、1 项真实 MySQL 集成测试跳过；Worker 48 项通过、2 项容器集成测试跳过。
- 任务 4 完成条件已满足：发布与取消之间的 check/use 竞态已关闭，规格审查与质量审查双重复审通过，全量测试和生产构建通过。
- 任务 7 尚未满足完成条件：普通测试通过不等于质量风险已关闭，必须完成下面列出的文件句柄、请求体、UUID、文件哈希和清理分页整改，再通过规格、质量和主流程三重验证。

### 当前阻塞与风险

1. **任务 7 下载连接提前断开可能泄漏文件句柄。** 控制器监听中止事件太晚，底层流又使用 `autoClose:false`。必须在数据库/文件查询前绑定 abort/close，并让同一个 `FileHandle` 在成功、失败、客户端断开时都幂等关闭。
2. **任务 7 合法确认请求可能被默认 100 KiB JSON 限制提前拒绝。** 需要端点级、认证前置、总大小明确的 JSON 解析策略；不能直接放大所有接口的全局上限。
3. **任务 7 嵌套 UUID 大小写未统一。** `draftQuestionId` 和 ACK 的 `artifactIds` 必须在业务比较与确认哈希计算前统一为小写，并覆盖仅大小写不同的重复 ID。
4. **任务 7 下载只校验数据库哈希格式和文件大小，没有校验文件实际 SHA-256。** 必须从同一个 `O_NOFOLLOW` 文件句柄流式计算哈希，验证后再从该句柄发送，防止同尺寸损坏文件被返回 200。
5. **任务 7 清理分页存在重复全量扫描。** MySQL 记录应使用真正的复合 keyset 查询，孤儿目录每轮只枚举一次，避免 3200 个分片加 500 个题图时重复扫描。
6. **任务 5–6 仍缺生产数据闭环。** `main.py` 要接入真实 `pdf_pipeline.PdfPipeline`；Worker 必须从任务记录取得源 PDF 和页码，按 manifest-first 保存题图，再原子写入草稿、题图元数据、进度与最终状态。
7. **服务器环境验证仍缺失。** hashed `requirements.txt`、PaddleOCR 模型层、PDFium、Linux 文件权限、真实 MySQL migration/锁竞争和容器资源限制尚未在 Ubuntu 服务器验证。
8. **真机验收仍缺失。** 当前 `hdc list targets` 为 `[Empty]`，Account Kit、私有 CA、沉浸光感、离线缓存、双设备文字同步和题图隔离都没有实机证据。

任务状态的唯一完成规则：先查看本文件；标记“已完成”的任务只做回归测试，不重复开发；标记“进行中/未开始”的任务按正式实施计划先写失败测试再实现；完成后必须把实际测试数量、构建结果、服务器或设备证据写回本文件。

## 3. 端侧既有能力

### 3.1 首页与导航

- 新增真实数据驱动的学习首页：待复习、错题总数、已掌握、掌握率、最近书籍和最近错题。
- 底部导航改为四项悬浮栏，顺序固定为“首页、书籍、错题、我的”。
- 使用 API 26 官方 `uiMaterial.ImmersiveMaterial`、系统 Symbol、选中绿色和未选中深灰。
- 首页、书籍、错题和我的页面保留独立刷新状态；前后台切换后可刷新数据并触发同步。

### 3.2 本地功能

- 题库、题目、搜索、编辑、删除、错题添加/移除、掌握状态和统计。
- JSON 题库导入。
- PDF 端侧导入：选页、渲染、OCR、切题、题图裁剪、确认修改和事务保存。
- PDF、OCR 中间结果和题图均保留在设备端，不进入云端同步载荷。
- 数据库当前为 schema version 5。

### 3.3 华为账号与会话

- “我的”页面使用 Account Kit 官方登录组件获取一次性 `authorizationCode`。
- 客户端只向 `/v1/auth/huawei` 发送授权码、deviceKey 和设备名称。
- Access Token 只保存在内存；Refresh Token 与 deviceKey 使用 Asset Store。
- 支持令牌刷新单飞、401 后刷新并重试一次、退出登录和会话恢复。
- 登录失败或云服务不可用不会阻止本地功能启动。

### 3.4 同步机制

- `sync_identity`：保持现有本地主键不变，映射到服务器使用的 UUID。
- `sync_outbox`：业务写入与同步事件处于同一 ArkData 事务，网络失败时不丢事件。
- `sync_state`：按用户保存服务器游标。
- push 每批最多 100 条；服务器确认后才删除对应 outbox 行。
- pull 按 `serverSequence` 应用，事务成功后才推进 cursor。
- 登录、回到前台和手动点击“立即同步”时触发；不申请后台常驻权限。
- 冲突基础规则为服务器序列最后写入生效。
- 旧版本已有的非示例题库、题目和错题状态会在升级后的首次启动中幂等进入待同步队列；内置示例题库不会上传。

## 4. 云端既有能力

云端目录为工程内的 `server/`，服务器部署目录为 `/opt/wqc`。

当前生产服务器已提供健康、认证和同步接口。下面 PDF 导入接口只存在于 HEAD `a9bb625` 的主分支代码，尚未发布到 `/opt/wqc`、尚未启用生产流量，也不得在任务 5–8 和任务 7 质量整改完成前对外使用：

```text
GET  /health
GET  /health/ready
POST /v1/auth/huawei
POST /v1/auth/refresh
POST /v1/auth/logout
POST /v1/sync/push
GET  /v1/sync/pull
POST /v1/imports/pdf
PUT  /v1/imports/pdf/:jobId/parts/:partIndex
POST /v1/imports/pdf/:jobId/complete
GET  /v1/imports/pdf/:jobId
GET  /v1/imports/pdf/:jobId/draft
POST /v1/imports/pdf/:jobId/confirm
GET  /v1/imports/pdf/:jobId/artifacts/:artifactId
POST /v1/imports/pdf/:jobId/artifacts/ack
DELETE /v1/imports/pdf/:jobId
```

已经完成：

- NestJS、TypeORM、MySQL 8.4、Caddy 和 Docker Compose 部署。
- 多用户数据隔离、华为标识加密保存、JWT Access/Refresh Token、Refresh Token 轮换。
- 题库、题目、错题状态和复习事件的增量同步数据模型。
- MySQL 不向公网发布 3306；公网业务入口仅为 HTTPS 443。
- 私有 CA 为公网 IP `114.132.197.160` 签发证书；端侧 HAP 内置公有证书 `ca.crt`，不包含 CA 私钥或服务器私钥。
- 服务器 `/health/ready` 已返回：`{"status":"ok","service":"wrong-question-api","database":"ready"}`。
- Client Secret 只属于服务器环境变量，不进入 HarmonyOS 客户端或 Git。

## 5. 2026-08-28 当前验证证据

以下结果均在主工程 `G:\code\openHarmony\wrong_question_collection` 本轮实际执行，不使用旧日志代替：

| 验证项 | 当前结果 | 结论 |
| --- | --- | --- |
| 端侧 Node 契约测试 | 19/19 个 `.test.cjs` 文件通过 | 现有端侧行为契约通过 |
| ArkTS/Hypium | Hvigor `test` 返回 `BUILD SUCCESSFUL` | ArkTS 单元测试任务通过 |
| API 26 HAP | Hvigor `assembleHap` 返回 `BUILD SUCCESSFUL` | 当前脏工作树仍可生成调试 HAP |
| NestJS Jest | 33/33 套件；316 项通过，1 项跳过 | 普通服务端回归通过；跳过项仍需真实 MySQL |
| NestJS 构建 | `tsc -p tsconfig.build.json` 退出码 0 | 服务端 TypeScript 生产构建通过 |
| Python Worker | 48 项通过，2 项跳过 | 纯逻辑/fake 引擎测试通过；PDFium 容器测试未执行 |
| 真机连接 | `hdc list targets` 返回 `[Empty]` | 本轮未执行安装、启动或真机 UI/网络验收 |

API 26 HAP 输出位置：

- `entry/build/default/outputs/default/entry-default-signed.hap`
- `entry/build/default/outputs/default/entry-default-unsigned.hap`

任务 7 当前提交链为 `37b01bc` → `df72258` → `a9bb625`。规格复审已通过，但质量复审明确“不批准”；因此 33 套件通过只证明现有自动化没有失败，不能覆盖已复现的文件句柄泄漏、100 KiB 请求体、嵌套 UUID、文件实哈希和清理复杂度问题。

当前 ArkTS 构建仍输出大量“函数可能抛异常”警告，并有 `getContext` 已弃用提示。它们没有阻止本轮 HAP 产出，但应在任务 9–14 修改对应文件时按模块清理，最终 release 构建前必须重新统计和确认。

## 6. 上线前待办

### P0-A：当前旧链路与基础能力回归

- [ ] 连接 HarmonyOS API 26 手机或模拟器，安装 `entry-default-signed.hap` 并冷启动。
- [ ] 检查四项底部导航、底部安全区、横竖屏、平板布局和沉浸光感交互。
- [ ] 在旧链路仍启用期间，验证未登录状态的本地 JSON/PDF 回退导入和数据保留；任务 14 切换后重新验证未登录只读/登录提示，不再要求未登录云端 PDF 导入。
- [ ] 使用真实华为账号验证 Account Kit 授权、服务端注册、刷新令牌和退出登录。
- [ ] 真机请求 `https://114.132.197.160`，确认 HAP 内置私有 CA 能完成 TLS 校验。
- [ ] 在旧链路仍启用期间验证 outbox 恢复；任务 12 切换后验证断网只读、写操作禁用和恢复联网后的刷新。

### P0-B：任务 5–14 完成后的云端最终验收

- [ ] 真机完成分片上传、断点续传、服务器 OCR、在线审核、确认、题图下载、哈希校验和 ACK。
- [ ] 第二台设备登录同一账号，验证文字题库和错题状态 pull；确认原 PDF 不作为跨设备资料同步，题图只在发起导入并完成下载的设备可用。
- [ ] 验证账号 A/B 在同一设备的文字缓存和题图映射隔离，退出后离线缓存行为符合任务 10 设计。
- [ ] 验证 24 小时超时、删除失败重试、API 重启恢复和磁盘压力拒绝新导入；不得影响正式文字与健康检查。

### P1：发布准备

- [ ] 在 AppGallery Connect 核对包名、签名证书 SHA-256、API 权限和华为登录配置。
- [ ] 使用真实参考 PDF 做 OCR 准确率、跨页切题、取消、内存峰值和长时间运行测试。
- [ ] 做 MySQL 备份恢复演练，不只检查备份文件生成。
- [ ] 补充隐私政策和账号说明：华为标识及学习文字长期保存在自建服务器；PDF、OCR 中间文件和待下载题图会临时上传/生成并在 ACK 或 24 小时后删除；题图长期仅保存在发起设备。
- [ ] 正式发布前优先申请域名并切换到公共受信任 CA；当前公网 IP 私有 CA 方案需要随 HAP 更新证书。
- [ ] 清理现有 ArkTS 编译警告，并执行 release 签名构建和发布包验收。

### P2：后续增强

- [ ] 同步历史、冲突提示、失败详情和按项重试。
- [ ] 云端账号注销、数据导出和数据删除。
- [ ] 异机加密备份、磁盘/接口告警、MySQL 慢查询监控。
- [ ] 根据真机样本继续优化数学 OCR 与复杂版式切题。

## 7. 下一步操作

正式逐项实施细节以 `docs/superpowers/plans/2026-08-26-server-pdf-processing-cloud-text-plan.md` 为准。实际执行必须遵循下面的顺序，不要先做页面再补后端，也不要在任务 7 未审查通过时部署到生产服务器。

### 7.1 第一步：关闭任务 7 的质量阻塞

1. 在 `import.controller` 调用下载服务前绑定请求 `aborted` 和响应 `close`，建立 `AbortSignal`，查询后再次检查 `destroyed`、`writableEnded` 和 signal 状态。
2. 在 `import-storage.service` 中让一个 `O_NOFOLLOW` 的 `FileHandle` 同时承担 `fstat`、SHA-256 校验和发送；流必须自动关闭，且 `end`、`error`、`close`、客户端中止都走幂等 close。
3. 为确认接口实现单独的 JSON 大小策略：认证和设备准入在缓存大请求体之前完成；确认接口上限建议 1–2 MiB；其他 JSON 仍保持 100 KiB；分片接口继续使用原始流和 4 MiB 限额。
4. 在 service 入口把路由 job ID、`draftQuestionId`、`artifactId`、ACK `artifactIds` 全部规范为小写 UUID；规范化后再查重并计算幂等确认哈希。
5. 把 cleanup repository 改为数据库复合 keyset `WHERE + ORDER BY + LIMIT`，禁止每页 `find` 全部再 JavaScript `slice`；孤儿目录每次 cleanup run 只 `readdir` 一次，并限制快照总量。
6. 先补失败测试，再实现修复。必须包含：重复提前断开无 FD 增长、合法约 115 KiB confirm 不返回 413、无效 token 在缓存正文前返回 401、超上限稳定 413、嵌套大写 UUID 成功、大小写重复 ID 被拒绝、同尺寸损坏题图不返回 200、3200 分片加 500 题图的线性分页。
7. 修复后依次执行：

```powershell
npm test --prefix server -- --runInBand
npm run build --prefix server
git diff --check
```

8. 规格复审、质量复审和主流程独立验证全部批准后，才能把任务 7 改为“已完成”，并在本文件记录新提交号和准确测试数量。

### 7.2 第二步：补齐任务 5–6 的真实 Worker 闭环

1. 删除 `server/worker/main.py` 内的占位 `PdfPipeline`，实例化 `pdf_pipeline.py` 的生产管线和真实 PDFium/PaddleOCR/Pillow 引擎。
2. 扩展 `JobStore`：领取任务时返回源 PDF 安全路径、页码范围、用户/设备和 claimedAt；所有更新都继续带 claimedAt 栅栏。
3. Worker 第一遍逐页渲染/OCR，持续写进度；第二遍裁图时先创建数据库 manifest，再原子发布文件，最后写入 artifact 的大小、SHA-256、题目关联和顺序。
4. 草稿、题图 metadata、状态 `review` 必须在同一业务提交边界内可恢复；崩溃后不得出现“磁盘文件存在但数据库完全不知道”或“数据库可下载但文件未发布”。
5. 在 Python 3.11/Linux 生成带哈希的 `requirements.txt`，不得用本机 Python 3.14 生成生产锁文件。
6. 在 Ubuntu/Docker 构建模型 stage 和 runtime stage，容器内运行全部 pytest 与两项 PDFium 集成测试，并用参考 PDF 记录页数、耗时、内存峰值和临时磁盘峰值。
7. 只有真实 Worker 从 MySQL 领取 → OCR → 写草稿/题图 → API 可读取 review 全链路通过，任务 5 和 6 才能标记完成。

### 7.3 第三步：完成任务 8 的服务器部署边界

1. 更新 `server/compose.yaml` 和 Caddy 配置，新增 `ocr-worker`，不开放任何公网端口。
2. Worker 只加入 MySQL/导入文件内部网络；不注入华为 Client Secret、JWT 密钥或 Caddy 私钥；只提供最小数据库运行账号。
3. 设置非 root、`read_only`、`cap_drop: ALL`、`no-new-privileges`、受限 `tmpfs`、CPU/内存限制、单副本和受控导入卷。
4. MySQL 继续不发布 3306，公网只保留 HTTPS 443；API 保留访问华为 OAuth 所需的出站网络。
5. 本地先通过 compose 合同测试；服务器再执行 `docker compose config --quiet`、构建和容器健康检查。完成前不得把 Worker 并入现有生产栈。

### 7.4 第四步：实现任务 9 的 HarmonyOS 云端导入传输

1. 新建严格 ArkTS 模型和 `CloudImportService`，实现 create、manifest、分片上传、complete、状态轮询、draft、confirm、artifact download、ACK、cancel。
2. 以 4 MiB 为最大分片，持久化 job ID、分片位图和源文件 SHA-256；应用重启后先查询服务器状态，再只补缺失分片。
3. 所有请求复用安全会话和 401 单次刷新；不记录 access token、PDF 内容或题目正文日志。
4. 下载题图到账号隔离临时目录，验证 Content-Length 和 SHA-256 后原子移动到设备正式目录；只有全部成功保存的 artifact 才发送精确 ACK。
5. 每次修改 `.ets` 后先做 ArkTS 检查，再执行 Hvigor test 和 assembleHap；这一顺序来自 `harmonyos-dev` 工作流，不能倒置。

### 7.5 第五步：任务 10，建立账号隔离的只读离线缓存

1. 数据库升级到 schema v6，只新增 `cloud_cache_state`、题库/题目/错题文字缓存和 `device_question_image` 映射，不删除 v5 旧表。
2. 下载一页文字与 cursor 必须在同一 ArkData RDB 事务提交；失败时保留上一份可读缓存。
3. 所有缓存行和题图映射包含 account ID；切换账号后绝不能查询到上一账号数据。
4. 退出登录时删除当前账号的文字缓存和 cursor，但保留该账号本机题图文件与隔离映射，等待同账号再次登录。
5. 做数据库升级、回滚保护、账号 A/B 隔离和离线启动测试，再构建 HAP。

### 7.6 第六步：任务 11–14，切换业务并迁移旧数据

1. **任务 11：页面切换。** PDF 设置、进度、审核页改用 `CloudImportService`；显示上传中、服务器识别中、等待确认、失败和图片过期；生产页面不再调用端侧 OCR/渲染。
2. **任务 12：服务器优先。** 在线写操作先请求 `/v1/sync/push`，成功后更新只读缓存；离线或未登录只允许浏览，编辑、删除、错题状态变更必须禁用并给出提示。
3. **任务 13：旧数据迁移。** 用户明确确认绑定当前华为账号后，按题库幂等上传旧文字；下载服务器快照核对数量与归一化文本 SHA-256；成功后重绑本机题图。任何一步失败都保留旧表和 outbox，可继续重试。
4. **任务 14：停用旧链路。** 只有任务 1–13 全部通过后，才移除端侧 OCR/PDF 生产引用并停止新建 outbox；旧表至少保留一个版本用于恢复，不做破坏性清理。

### 7.7 第七步：任务 15，服务器与真机最终验收

1. 部署前检查 Ubuntu 至少 2 核、4 GB RAM（或 RAM 加受控 swap）和 `/opt/wqc` 至少 15 GB 可用空间；先完成 MySQL 逻辑备份与配置备份。当前单 PDF 上限是 200 MiB、默认分片 4 MiB、磁盘最小保留水位 5 GiB、Worker 单并发。部署前还要定义每用户/全局活动任务数上限，并按每个任务至少“分片 + 合并 PDF + OCR 中间文件 + 题图”三份 200 MiB 级预算做压力测试；40 GB 系统盘必须同时扣除 Docker 镜像、Paddle 模型、MySQL 和备份占用。
2. 在服务器依次验证 Compose 配置、构建 API/migrate/Worker、执行 migration，再启动 API、Worker、Caddy 和 backup；确认 MySQL/API healthy、migration 退出 0、Worker 无公网端口。
3. 用测试账号完成一份小 PDF 的 create → upload → complete → OCR → review → confirm → 原设备下载 → ACK；核对下载哈希，ACK 后临时文件不可再下载而正式文字仍存在。
4. 第二台设备登录同一账号，只能同步文字，不能下载第一台设备的临时题图；不同账号在服务端和同一设备上都必须完全隔离。
5. 验证断网后文字缓存可读、所有写按钮禁用；恢复网络后刷新正常。验证同账号退出再登录题图可见，不同账号不可见。
6. 用可注入时钟或隔离环境短 TTL 验证 24 小时清理、孤儿目录限制和磁盘压力拒绝新导入；正式题库文字与健康检查不能受影响。
7. 真机操作顺序：

```powershell
& 'D:\Program Files\Huawei\DevEco Studio1\sdk\default\openharmony\toolchains\hdc.exe' list targets
& 'D:\Program Files\Huawei\DevEco Studio1\sdk\default\openharmony\toolchains\hdc.exe' install -r `
  'G:\code\openHarmony\wrong_question_collection\entry\build\default\outputs\default\entry-default-signed.hap'
& 'D:\Program Files\Huawei\DevEco Studio1\sdk\default\openharmony\toolchains\hdc.exe' shell aa start `
  -a EntryAbility -b com.gfs.wrongquestion
```

8. 发现崩溃时先保存 `hdc hilog` 和 faultlogger 证据，不得通过删除数据库掩盖升级或同步问题。
9. 最终把测试数量、容器状态、设备 ID、OCR 耗时/内存/磁盘、双设备结果和未验证项写回本文件；没有观察证据的项目不得勾选完成。

### 7.8 每个任务完成后的固定收尾

1. 重新读取本文件，确认本任务当前不是“已完成”；若已完成，仅复测。
2. 运行该任务的 focused tests、全量回归、生产构建和 `git diff --check`。
3. 检查 `git status --short`，只暂存该任务文件；保留当前 `entry/` 的用户未提交修改、其他工程目录和部署压缩包。
4. 扫描 `.env`、私钥、PDF、题图、模型缓存、备份、签名材料和 Client Secret，确认都未进入 Git。
5. 更新本文件中的状态、实际证据、剩余风险和下一步，然后再提交文档。
